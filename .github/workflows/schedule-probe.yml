# 예약(schedule) 트리거가 이 계정의 다른 저장소에서는 동작하는지 확인하는 진단용.
# 아무 작업도 하지 않으며, 확인이 끝나면 삭제한다.
name: schedule-probe

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "triggered by ${{ github.event_name }}"
